// interfaces/TeamInfo.ts
export interface TeamInfo {
  icon: string;
  name: string;
  description: string;
}

// interfaces/ServiceInfo.ts
export interface ServiceInfo {
  icon: string;
  name: string;
  description: string;
}

export interface TestimonalInfo {
  stars: string;
  review: string;
  description: string;
  company: string;
  name: string;
  role: string;
}

// interfaces/WhyUs.ts
export interface TechExpertiseInfo {
  title: string;
  description: string;
}

// interfaces/CrewInfo.ts
export interface CrewInfo {
  profileImage: string;
  role: string;
}
